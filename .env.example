# Postgres is NOT started by this docker-compose.yaml -- it already runs on
# the VPS (~/db/docker-compose.yaml). Point these at that running instance.
DB_HOST=host.docker.internal
DB_PORT=5432
DB_USER=lomba
DB_PASSWORD=change_me
DB_NAME=lomba_challenge
